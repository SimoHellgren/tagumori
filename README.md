# filetags
Allows arbitrarily nested tags for files.

# development
Use `wsl` if on Windows.

There is logic built on files' inode and device, which are not reliable on Windows. For now, I have no intention to make this cross-platform.
